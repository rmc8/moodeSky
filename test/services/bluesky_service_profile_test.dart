// Package imports:
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('BlueskyService Profile', () {
    test('Service refactoring in progress', () {
      // Service tests are temporarily disabled
      // These tests will be re-enabled when service refactoring is complete
      expect(true, isTrue);
    });
  });
}
