// Package imports:
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AuthNotifier.addAccount', () {
    test('OAuth functionality temporarily frozen', () {
      // OAuth tests are temporarily disabled
      // These tests will be re-enabled when OAuth implementation is ready
      expect(true, isTrue);
    });
  });
}